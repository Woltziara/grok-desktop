# 同一主线续工记录 · 2026-09-14

## 恢复来源

从 GitHub Actions 34844464637 的归档恢复实际提交 `62d49e3c06ad8cbaa9c14ee0ff30289a7c8029bb`。
归档 SHA-256：`12b862c842dbe3c75a7b457f26e55655caa07483288c182a1e91cb66d27f6f13`。
恢复树的 Git tree SHA 为 `37e18cd489535e23bfdb878f5142ac9e6a7579d1`，与 GitHub commit API 一致。
当前可访问容器未发现此前未提交源码；仅保留旧源码/依赖归档。已入库的生命周期成果保留；本记录以下内容是本轮实际新实施，不称为找回了旧未提交成果。

## 单元一：取消恢复与精确原话来源

- 普通明确的 prompt RPC 失败不再把整个连接当作已取消；限流、认证和服务错误之后，反向请求与下一次 prompt 仍可使用同一连接。
- 隔离旧 transport 与隐藏 session/load 回放分开。新连接可接收 folder trust 和 MCP elicitation；旧 reader 不能污染新连接。
- 恢复加载失败保留原会话 id；下一次重试重新启动连接。恢复中再次取消不会被完成回调抹掉。
- 指定 inboxIds 时先按事件 id 限定来源，再匹配原话；不会以同文第一条替代第二条或另一个会话来源。不明确指定的旧单输入格式仅回落 ctx.inboxId，不扩大搜索。
- 实测环境：Linux x64，Node 22.16.0，npm 10.9.2；依赖来自原 GitHub CI 的锁定依赖归档。
- 执行：`node --test test/transport-recovery.test.mjs test/desktop-lifecycle-regressions.test.mjs`，18/18；`npm run check`，类型检查与712/712测试；`npm run build`，成功。
- `transport-recovery` 是真实 child process/stdin/stdout/start/load 生命周期 + 测试专用 JSON-RPC peer。没有真实 Grok 账号，不把它称为真实模型验收。

## 尚待本次继续实现

跨项目会话完整迁移、Preview跨会话归属、工作认识跨机冲突与入口、网页Pro开发往返及相关真实消费者检查。分批保存不代表总任务完成。真实macOS/本人账号/第二机器验收另记。
